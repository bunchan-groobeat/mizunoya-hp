# 進行メモ 2026-07-10（ターミナル太郎 → ジロー）

指示書 `mizunoya-hp-renewal-shijisho.md` は**原文のまま**。以下はその後の社長判断による補足。矛盾する場合は**このメモが優先**する。

## 1. 指示書第6章の前提は現時点で成立していない

指示書は「`assets/` に看板のAIデータを配置済み」としているが、**リポジトリ内にAI/EPSファイルは1つも存在しない**（2026-07-10 検証済み）。

したがって**着手順を変更する**（社長承認済み・B案）。

| | 指示書の当初順 | 実際の着手順 |
|---|---|---|
| ① | 素材SVG化 | **保留**（AIデータ到着後に実行） |
| ② | トップページ仮組み | **ここから着手** |
| ③ | 検収依頼 | 変更なし |

## 2. 仮素材の命名規則

AIデータ到着まで、以下の命名で仮ファイルを置き、到着後に差し替える。

- カタカナロゴ「ミズノヤシドウシャ」→ `logo-temp.svg`
- KURU MAYAバッジ → `badge-temp.svg`
- ポルシェイラスト → `porsche-temp.png`（指示書第5章の命名規則どおり。ライセンス確認中のため差し替え・削除を容易に）

**すべて `-temp` を含む名前にすること。**差し替え漏れをgrepで検出できるようにするため。

## 3. 参考画像の使い分け（`docs/references/`）

| ファイル | 位置づけ | 扱い |
|---|---|---|
| `01_看板.jpg` | **配色と世界観の正** | 赤・紺・白の基準はここから取る |
| `02_funchase構成.jpg` | 構成と余白の**参考のみ** | レイアウトの考え方だけ。**流用不可** |
| `03_subikusu雰囲気.jpg` | 構成と余白の**参考のみ** | 同上。**流用不可** |

02・03の写真・文章・ロゴ・配色をそのまま持ち込むことは**禁止**。

## 4. 未確定のまま進めるもの

- Formspree受信先メール → テストIDで仮組み
- Behold.so Feed ID（@yoshimasa_mizunoya）→ 枠のみ
- ポルシェイラストのライセンス → 仮使用。**検収前に社長が可否を確定させる**
