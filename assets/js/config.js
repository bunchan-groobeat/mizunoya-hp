/* =========================================================
   水野谷自動車 サイト設定ファイル（接続情報）
   ---------------------------------------------------------
   お知らせ/ブログ を microCMS から読み込むための接続情報です。
   J-DRY(jdry-hp) と同方式。社長から下記2つの値を受け取ったら、
   ここを書き換えるだけで自動取得に切り替わります。

   ★ 設定するのは次の2つだけ ★
     1) MICROCMS_SERVICE_DOMAIN … microCMS のサービスドメイン
          例: サービスのURLが https://mizunoya.microcms.io なら "mizunoya"
     2) MICROCMS_API_KEY … 「読み取り専用（GETのみ）」のAPIキー

   ⚠️ セキュリティ上とても大切なこと ⚠️
   - 静的サイトのため、ここに書いたAPIキーは閲覧者のブラウザから
     見える状態になります（技術的に隠せません）。
   - そのため、ここに置いてよいのは必ず「読み取り専用（GET権限のみ）」の
     APIキーだけです。書き込み・管理権限のキーは絶対に置かない／
     GitHubにコミットしないでください。
   ---------------------------------------------------------
   ★現状：未接続（空欄）。空欄の間は HTML に書いてあるダミー記事を
     そのまま表示します（キー到着前でもページは成立します）。
========================================================= */

window.MIZUNOYA_CONFIG = {
  /* microCMS のサービスドメイン（サブドメイン部分のみ）。未設定なら空のまま。 */
  MICROCMS_SERVICE_DOMAIN: '',

  /* 読み取り専用（GETのみ）のAPIキー。未設定なら空のまま。 */
  MICROCMS_API_KEY: '',

  /* microCMS で作成する API（エンドポイント）のID。スキーマ設計どおり "blog"。 */
  MICROCMS_ENDPOINT: 'blog',

  /* 一覧に表示する最大件数。 */
  BLOG_LIMIT: 20
};
